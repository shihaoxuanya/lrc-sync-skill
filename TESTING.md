# v1.0.0 测试记录

执行日期：2026-09-08。以下是本次构建的实际运行结果，不是所有平台兼容性或歌曲同步精度的保证。

| 检查 | 结果 | 范围 |
| --- | --- | --- |
| Python 单元测试 | 51 项通过 | 时间格式、边界、舍入冲突、原稿覆盖、重复行、offset、偏移、导出、HTML 注入防护、中文路径 |
| 浏览器交互检查 | 12 组通过 | 渲染、本地音频、微调、撤销、越界拒绝、复核状态、JSON 保存恢复、LRC 下载、播放高亮及速度、清屏、移动端、外部请求 |
| 实际录音结构回归 | 通过 | 对本次录音与修正版 LRC 检查：66 行正文、69 项时间轴，导入导出无丢失，未把候选标成已试听 |
| 实际音频信息 | 通过 | FFprobe 测得时长 289.697959 秒并计算 SHA-256；错误时长和错误指纹的拒绝逻辑另有单元测试 |
| 波形与声谱 | 通过 | 用本地 12 秒合成提示音生成图片，横轴采用原音频绝对秒数 |
| 内嵌页面生成 | 通过 | 可选内嵌合成音频；默认页面不内嵌 |
| Skill 与 YAML | 通过 | 名称匹配目录，配置文件可以解析 |
| 本地安装 | 通过 | 复制到隔离测试目录，未改动已有 Skill |

## 测试不能证明什么

没有为真实歌曲计算或保证毫秒级同步误差。真实录音回归仅验证结构和完整性；导入的 `verified: false` 不会自动变成已试听确认。

浏览器测试使用 Chromium，将 HTML 注入空白测试页，不需要网络导航。它不能证明真实文件来源下的 `localStorage` 行为、所有浏览器的音频编解码支持或所有播放器的外置歌词表现。缓存、内嵌歌词优先级与非零 offset 约定仍需在目标播放器中核实。

回归使用的真实录音、完整歌词、项目 JSON 和含音频 HTML **不随源码发布**。这里只包含合成提示音生成代码和非歌曲示例。
